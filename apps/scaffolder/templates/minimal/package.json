{
  "name": "{{PACKAGE_NAME}}",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "build": "bun ./node_modules/@teyik0/furin/src/cli/index.ts build --target bun",
    "test:types": "tsc --noEmit"
  },
  "dependencies": {
    "@teyik0/furin": "{{FURIN_VERSION}}",
    "bun-plugin-tailwind": "{{BUN_PLUGIN_TAILWIND_VERSION}}",
    "elysia": "{{ELYSIA_VERSION}}",
    "react": "{{REACT_VERSION}}",
    "react-dom": "{{REACT_DOM_VERSION}}"
  },
  "devDependencies": {
    "@types/bun": "{{TYPES_BUN_VERSION}}",
    "@types/react": "{{TYPES_REACT_VERSION}}",
    "@types/react-dom": "{{TYPES_REACT_DOM_VERSION}}",
    "tailwindcss": "{{TAILWIND_VERSION}}",
    "typescript": "{{TYPESCRIPT_VERSION}}"
  }
}
